ulimit  -c
echo /home/saoge/testCore/data/coredump/core.%e.%p> /proc/sys/kernel/core_pattern
core文件生成条件段错误
gdb 调试过程
gdb ./a.out ./core文件
定位代码崩溃位置
查看coredump时的堆栈

查看堆栈使用bt或者where命令
但往往正常发布环境是不会带上调试信息的，因为调试信息通常会占用比较大的存储空间，一般都会在编译的时候把-g选项去掉。
没有调试信息的情况下找core的代码行
在带上调试信息的情况下，我们实际上是可以看到core的地方和代码行的匹配位置
没有调试信息的情况下，打开coredump堆栈，并不会直接显示core的代码行
disassemble
此时，frame addr(帧数)或者简写如上，f 1 跳转到core堆栈的第1帧。因为第0帧是libc的代码，已经不是我们自己代码了。
